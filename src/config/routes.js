const admin = {
  dashboard: '/admin',
  appointments: '/admin/appointments',
  doctors: '/admin/doctors',
  patients: '/admin/patients',
  users: '/admin/users',
};

const routes = {
  home: '/',
  login: '/login',
  signup: '/signup',
  admin,
};

export default routes;
